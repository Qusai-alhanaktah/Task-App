- if params[:controller] == 'tasks' && current_user
  #realEstateMenu.top-bar
    .top-bar-left
      %ul.menu
        %li= link_to 'Logistaas',root_path, class:"menu-text"
        %li= link_to "Home", root_path
        %li= link_to "Add New", new_task_path
    .top-bar-right
      %ul.menu
        %li= render 'partials/search'
        %ul.dropdown.menu{"data-dropdown-menu" => ""}
          %li
            %a{:href => "#"}
              Hi, #{current_user.email.split('@')[0].capitalize}
            %ul.menu
              %li= link_to "Settings", edit_user_registration_path(current_user)
        %li= link_to 'Sign Out',  destroy_user_session_path, mehtod: :delete, class:"button"
- else
  #realEstateMenu.top-bar
    .top-bar-left
      %ul.menu
        %li= link_to 'Logistaas',root_path, class:"menu-text"
        %li= link_to "Home", root_path
    .top-bar-right
      %ul.menu
        %li= link_to 'Sign Up', new_user_registration_path ,class:"button"
        %li= link_to 'LogIn', new_user_session_path,class:"button"
